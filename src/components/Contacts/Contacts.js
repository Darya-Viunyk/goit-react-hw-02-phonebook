import { User } from './User';
// import PropTypes from 'prop-types';

export const Contacts = () => {
  return (
    <did>
      <h2>Contacts</h2>
      <ul>
        {/* {users.map(({ id, name, number }) => {
        return (
          <li key={id}>
            <User name={name} number={number} deleteUser={deleteUser} id={id} />
          </li>
        );
      })} */}
      </ul>
    </did>
  );
};
// UsersList.propTypes = {
//   users: PropTypes.arrayOf(
//     PropTypes.shape({
//       id: PropTypes.string.isRequired,
//     }).isRequired
//   ).isRequired,
// };
